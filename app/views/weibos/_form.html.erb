<%= form_for(weibo) do |f| %>
  <% if weibo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weibo.errors.count, "error") %> prohibited this weibo from being saved:</h2>

      <ul>
      <% weibo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
